behaviors:
  DroneAgent:
    trainer_type: ppo

    hyperparameters:
      # Hyperparameters common to PPO and SAC
      batch_size: 2048
      buffer_size: 20480
      learning_rate: 1e-5
      learning_rate_schedule: linear


      # PPO-specific hyperparameters
      beta: 5.0e-3
      beta_schedule: constant
      epsilon: 0.2
      epsilon_schedule: linear
      lambd: 0.95
      num_epoch: 5
      shared_critic: False


    # Configuration of the neural network (common to PPO/SAC)
    network_settings:
      vis_encode_type: simple
      normalize: false
      hidden_units: 128
      num_layers: 2
      # memory
      memory:
        sequence_length: 64
        memory_size: 256

    # Trainer configurations common to all trainers
    max_steps: 5000000
    time_horizon: 64
    summary_freq: 10000
    keep_checkpoints: 5
    checkpoint_interval: 100000
    threaded: false
    init_path: null

    # behavior cloning
    behavioral_cloning:
      demo_path: F:\Unity_projects\Oblig2\Drone_MLagents\Demos\DroneAgentThis_0.demo
      strength: 0.5
      steps: 400000
      batch_size: 2048
      num_epoch: 5
      samples_per_update: 0

    reward_signals:
      # environment reward (default)
      extrinsic:
        strength: 1.0
        gamma: 0.99
      curiosity:
        strength: 0.2
        gamma: 0.99
        encoding_size: 256
        learning_rate: 1e-5

      # GAIL
      gail:
        strength: 0.5
        gamma: 0.99
        encoding_size: 128
        demo_path: F:\Unity_projects\Oblig2\Drone_MLagents\Demos\DroneAgentThis_0.demo
        learning_rate: 1e-5
        use_actions: false
        use_vail: false
